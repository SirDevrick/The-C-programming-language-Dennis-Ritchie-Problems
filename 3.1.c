/* N uestra búsqueda binaria realiza dos pruebas dentro del ciclo, */
/* cuando un a podría ser suficiente (al precio de más pruebas en el exterior).
 * Escriba */
/* una versión con sólo una prueba dentro del ciclo y m ida la diferencia en
 * tiempo */
/* de ejecución. */

#include <stdio.h>

int main() {
  printf("Hello world\b");
  return 0;
}
